```
    |\
4(a)| \5(c)
    |_(\A
    3(b)

cos(A) = 3/5，cos 0°= 1，cos 90°= 0
sin(A) = 4/5，sin 0°= 0
tan(A) = 4/3

任意三角形三边关系的余弦定理：a² = b² + c² - 2bc·cosA


m×n矩阵A也记作Amn，m行n列，Amn x Bnp = Cmp
```

范数是一个函数，将向量映射到一个非负实数，满足以下性质：
1) 非负性：||x|| ≥ 0，且 ||x|| = 0 当且仅当 x = 0
2) 齐次性：||αx|| = |α|·||x||（α为标量）
3) 三角不等式：||x + y|| ≤ ||x|| + ||y||

范数有几种：
- L2范数（默认）/欧几里得范数：向量长度/模
||a||₂ = ||a|| = (x1^2 + y1^2 + z1^2 + ...)^(1/2)
- 平方范数（数学计算上更高效），值就是L2范数的平方
||a||^2 = x1^2 + y1^2 + z1^2 + ...
- L1范数（曼哈顿范数）
||x||₁ = |x₁| + |x₂| + ... + |xₙ| 


向量内积（点积）：a、b两个向量的内积表示成<a,b>：
<a,b> = xa*xb + ya*yb + ... = (a在b方向上的投影长度)*||b|| = ||a||*||b||*cosθ 


- BLAS（Basic Linear Algebra Subprograms 的缩写）
是用于执行基础线性代数操作的标准化接口和库，常见的实现有Intel MKL（Math Kernel Library）、cuBLAS；

