**4. Go 的接口（Interface）是怎么实现的？什么是“鸭子类型”（Duck Typing）？**
*   **考察点**：面向对象设计在Go中的体现。
*   **参考要点**：
    *   鸭子类型：不需要显式声明实现某个接口（如Java的`implements`），只要实现了接口定义的所有方法，就认为实现了该接口（隐式实现）。
    *   底层结构：包含动态类型（Type）和动态值（Value）。（可引申出 `nil` 接口和包含 `nil` 值的接口的区别）。